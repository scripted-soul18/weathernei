{
  "name": "bharat-netra-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Bharat Netra — Safe Road Navigation & Weather Hazard Prediction Platform",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "python run.py"
  }
}
